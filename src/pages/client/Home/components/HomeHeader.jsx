import { Affix, Button, Col, Row } from "antd"
import { memo } from "react"
import {  HeaderClient, SearchIcon } from '../../../../components'


function HomeHeader({

}) {
    return (
        <HeaderClient />
    )
}

export default memo(HomeHeader)